<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.house.healthMgmt">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_app_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ProteinActivity" />

        <activity android:name=".ErrorActivity" />

        <activity android:name=".FoodActivity" />
		
		<activity android:name=".WeightActivity" />
		
		<activity android:name=".SodiumActivity" />
		
		<activity android:name=".WaterActivity" />
		
		<activity android:name=".WaterTargetActivity" />
		
		<activity android:name=".WaterTypeActivity" />
		
		<activity android:name=".NoBeverageActivity" />
		
		<activity android:name=".BeverageTypeActivity" />
		
		<activity android:name=".NoAlcoholActivity" />
		
		<activity android:name=".AlcoholTypeActivity" />
		
		<activity android:name=".SleepActivity" />
		
		<activity android:name=".SleepTypeActivity" />
		
		<activity android:name=".SleepTargetActivity" />
		
		<activity android:name=".ExerciseActivity" />
		
		<activity android:name=".ExerciseTypeActivity" />
		
		<activity android:name=".ExerciseTargetActivity" />
		
		
		<!-- ✅ 이 줄 추가 -->
<activity android:name=".MonthlyReportActivity" />

    </application>

</manifest>







